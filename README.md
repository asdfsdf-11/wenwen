# wenwen
多平台内容自动整理与周报生成 Agent。支持从本地 Git 仓库、GitHub PR/Issue、自定义条目等多数据源采集工作记录，按   conventional commits 规范自动分类（feat/fix/refactor/docs 等），输出 Markdown / HTML /   纯文本格式的结构化周报。零外部依赖，纯 Python 标准库实现，支持 CLI 直接使用或 JSON 配置文件驱动多仓库批量生成
